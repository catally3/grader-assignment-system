import styled from "@emotion/styled";
import Layout from "../../layouts/Layout.js";

const Button = styled.button`
  color: hotpink;
`;

const CourseManagement = () => {
  return (
    <Layout>
      <Button>CourseManagement</Button>
    </Layout>
  );
};

export default CourseManagement;
