import styled from "@emotion/styled";
import Layout from "../../layouts/Layout.js";

const Button = styled.button`
  color: hotpink;
`;

const GraderAssignment = () => {
  return (
    <Layout>
      <Button>GraderAssignment</Button>
    </Layout>
  );
};

export default GraderAssignment;
