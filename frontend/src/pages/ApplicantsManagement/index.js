import styled from "@emotion/styled";
import Layout from "../../layouts/Layout.js";

const Button = styled.button`
  color: hotpink;
`;

const ApplicantsManagement = () => {
  return (
    <Layout>
      <Button>ApplicantsManagement</Button>
    </Layout>
  );
};

export default ApplicantsManagement;
